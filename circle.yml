machine:
  timezone:
    Asia/Tokyo
  php:
    version: 5.6.5

dependencies:
  override:
    - composer install --prefer-source --no-interaction --no-dev

test:
  override:
    - laravel/vendor/bin/phpunit


